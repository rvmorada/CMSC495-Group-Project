//import static org.junit.jupiter.api.Assertions.*;
//import org.junit.jupiter.api.Test;

// Disabled test class as it is no longer functional, but Maria is not familiar with
// this library. Further development needed. 
class DateCheckerTest {

	//@Test
	void testIsValidDate() {
		DateChecker dateChecker = new DateChecker();
		
//		assertEquals(true, dateChecker.isValidDate("09/26/2019"));
//		assertEquals(false, dateChecker.isValidDate("09/32/2019"));
	}

}
